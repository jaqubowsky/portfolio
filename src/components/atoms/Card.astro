---
import { cn } from "@utils/index";

const {class: className, ...rest} = Astro.props;
---

<div class={cn("bg-popover text-popover-foreground px-5 py-4 rounded-md border-2 border-primary-foreground min-w-[300px]", className)} {...rest}>
  <slot />
</div>
