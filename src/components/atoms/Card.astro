---
import { cn } from "@utils/cn";

const { class: className, ...rest } = Astro.props;
---

<div
  class={cn(
    "bg-card text-card-foreground px-5 py-4 rounded-xl border border-border",
    className,
  )}
  {...rest}
>
  <slot />
</div>
