---
import { cn } from "@utils/cn";

const { class: className, ...rest } = Astro.props;
---

<a
  {...rest}
  class={cn(
    "text-foreground cursor-pointer font-medium transition-colors hover:text-primary focus:text-primary max-w-max",
    className,
  )}
>
  <slot />
</a>
